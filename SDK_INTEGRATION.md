# Интеграция SDK для RuStore и VK Ads

## Добавленные SDK

### 1. VK Ads SDK (myTarget)
- **Зависимость**: `com.my.target:mytarget-sdk:5.18.0`
- **Репозиторий**: `https://mvn.my.com`
- **Назначение**: Показ рекламных баннеров и нативной рекламы

### 2. RuStore SDK
- **Статус**: Требуется добавление после получения доступа к репозиторию
- **Назначение**: Интеграция с платёжной системой RuStore и основными функциями магазина

## Рекламные блоки

### Баннер 320x50
- **ID блока**: 1936811
- **Расположение**: Внизу главной страницы (фиксированный)
- **Компонент**: `VKAdsBanner`
- **Размеры**: 320x50px

### Нативный блок
- **ID блока**: 1936883
- **Расположение**: В модальном окне AdPage
- **Компонент**: `VKAdsNative`
- **Тип**: Нативная реклама (адаптируется под дизайн)

## Разрешения Android

Добавлены следующие разрешения в `AndroidManifest.xml`:
- `INTERNET` - для загрузки рекламы
- `ACCESS_NETWORK_STATE` - проверка состояния сети
- `ACCESS_WIFI_STATE` - проверка Wi-Fi
- `READ_PHONE_STATE` - для таргетинга рекламы (опционально)
- `ACCESS_COARSE_LOCATION` - для геотаргетинга (опционально)
- `ACCESS_FINE_LOCATION` - для точного геотаргетинга (опционально)

## Следующие шаги

1. **RuStore SDK**: 
   - Получить доступ к репозиторию RuStore SDK
   - Добавить зависимость в `build.gradle`
   - Интегрировать платёжную систему

2. **Тестирование рекламы**:
   - Проверить работу баннера 320x50 на главной странице
   - Проверить работу нативного блока в модальном окне
   - Убедиться, что реклама загружается корректно

3. **Оптимизация**:
   - Настроить таргетинг рекламы
   - Оптимизировать частоту показа
   - Добавить обработку ошибок загрузки рекламы

## Реализация

### Нативная интеграция VK Ads SDK

Интеграция выполнена согласно официальной документации:
- Создан нативный Capacitor плагин `VKAdsPlugin.java`
- Используется `MyTargetView` для показа баннеров
- Баннер добавляется программно в контейнер внизу экрана
- Поддерживаются форматы: 320x50, 300x250, 728x90, адаптивный

### Компоненты

- `VKAdsBanner.tsx` - React компонент, который вызывает нативный плагин
- `VKAdsPlugin.java` - нативный Android плагин для работы с MyTargetView
- `MainActivity.java` - содержит контейнер для баннера

## Примечания

- ✅ VK Ads SDK интегрирован нативно через Capacitor плагин
- ✅ Используется MyTargetView согласно документации
- ✅ Контейнеры для рекламы добавлены в иерархию представлений
- ⚠️ RuStore SDK требует регистрации в кабинете разработчика RuStore

## Готовность к публикации в RuStore

### ✅ Что уже готово:
1. **VK Ads SDK полностью интегрирован**:
   - SDK добавлен в зависимости (`build.gradle`)
   - Репозиторий настроен (`https://mvn.my.com`)
   - Нативный плагин реализован (`VKAdsPlugin.java`)
   - Компоненты React созданы (`VKAdsBanner.tsx`, `VKAdsNative.tsx`)
   - Контейнеры добавлены в MainActivity
   - Разрешения Android добавлены в манифест

2. **Рекламные блоки настроены**:
   - Баннер 320x50 (ID: 1936811) - на главной странице
   - Нативная реклама (ID: 1936883) - в модальном окне

### ⚠️ Важные моменты перед публикацией:

1. **Проверка ID рекламных блоков**:
   - Убедитесь, что ID блоков (1936811 и 1936883) активны в вашем кабинете VK Ads
   - Проверьте, что блоки настроены для мобильных приложений

2. **Тестирование**:
   - Протестируйте приложение на реальном устройстве перед публикацией
   - Убедитесь, что реклама загружается и отображается корректно
   - Проверьте работу в разных сценариях (с интернетом и без)

3. **RuStore SDK** (опционально):
   - RuStore SDK не обязателен для показа рекламы
   - Он нужен только для интеграции платёжной системы RuStore
   - Реклама будет работать и без RuStore SDK

### 🚀 После публикации:
После добавления новой версии в RuStore реклама должна автоматически начать показываться, если:
- ✅ Приложение установлено на устройство
- ✅ Есть активное интернет-соединение
- ✅ Рекламные блоки активны в кабинете VK Ads
- ✅ Есть доступная реклама для показа

