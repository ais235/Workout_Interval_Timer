# Безопасность проекта

## ✅ Защищённые файлы (в .gitignore)

Следующие файлы **НЕ** попадут в GitHub:
- `workout-timer-release.keystore` - файл с ключом подписи APK
- `keystore.properties` - пароли от keystore
- `node_modules/` - зависимости
- `.env.local` - локальные переменные окружения (если используется)

## 📝 SDK ключ MyTracker

**SDK ключ `65669749871971808692` находится в `index.html`** и будет виден в репозитории.

### Это безопасно?

**ДА, это безопасно!** Вот почему:

1. **Это публичный идентификатор** - SDK ключ MyTracker - это не секретный ключ, а публичный идентификатор вашего приложения
2. **Он виден всем** - любой пользователь может открыть исходный код вашего приложения в браузере и увидеть этот ключ
3. **Это нормальная практика** - большинство аналитических SDK (Google Analytics, Yandex Metrica, MyTracker) используют публичные ключи
4. **Защита на стороне сервера** - MyTracker защищает ваш аккаунт через личный кабинет, а не через скрытие ключа

### Что НЕ нужно публиковать:

❌ **Keystore файл** - это секретный ключ для подписи APK (уже в .gitignore)
❌ **Пароли от keystore** - критически важные данные (уже в .gitignore)
❌ **Секретные API ключи** - если бы они были (например, для платежей)

### Если всё равно хотите скрыть SDK ключ:

Можно вынести его в переменные окружения, но это избыточно для публичного ключа. Если нужно, могу показать как.

## 🔒 Рекомендации

1. ✅ Keystore файлы уже защищены в .gitignore
2. ✅ SDK ключ можно оставить в коде - это безопасно
3. ✅ Регулярно проверяйте, что keystore не попал в git: `git status`






