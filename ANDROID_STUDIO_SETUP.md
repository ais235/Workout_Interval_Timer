# Инструкция по настройке Android Studio и сборке APK

## 📋 Содержание
1. [Установка Android Studio](#установка-android-studio)
2. [Настройка Android Studio](#настройка-android-studio)
3. [Открытие проекта в Android Studio](#открытие-проекта-в-android-studio)
4. [Сборка APK файла](#сборка-apk-файла)
5. [Установка APK на устройство](#установка-apk-на-устройство)
6. [Решение проблем](#решение-проблем)

---

## 🚀 Установка Android Studio

Если Android Studio еще не установлен:

1. **Скачайте Android Studio:**
   - Перейдите на https://developer.android.com/studio
   - Скачайте версию для Windows
   - Запустите установщик

2. **Установите Android Studio:**
   - Следуйте инструкциям установщика
   - При первом запуске выберите "Standard" установку
   - Установятся:
     - Android SDK
     - Android SDK Platform-Tools
     - Android Emulator
     - Intel HAXM (если нужно)

3. **Проверьте установку SDK:**
   - Путь к SDK обычно: `C:\Users\<ВашеИмя>\AppData\Local\Android\Sdk`
   - Файл `android/local.properties` уже создан автоматически

---

## ⚙️ Настройка Android Studio

### Шаг 1: Открыть проект

1. Запустите Android Studio
2. Выберите **"Open"** (или "File" → "Open")
3. Перейдите в папку проекта: `D:\project\Workout_Interval_Timer\android`
4. Нажмите **"OK"**

**Важно:** Открывайте папку `android`, а не корневую папку проекта!

### Шаг 2: Дождаться синхронизации Gradle

- Android Studio автоматически начнет синхронизацию проекта
- Внизу появится прогресс-бар "Gradle Sync"
- Это может занять 5-10 минут при первом запуске (скачиваются зависимости)
- Дождитесь завершения синхронизации

### Шаг 3: Установить необходимые SDK компоненты

Если Android Studio попросит установить компоненты:

1. Откройте **"File"** → **"Settings"** (или `Ctrl+Alt+S`)
2. Перейдите в **"Appearance & Behavior"** → **"System Settings"** → **"Android SDK"**
3. Убедитесь, что установлены:
   - ✅ **Android SDK Platform 35** (API Level 35)
   - ✅ **Android SDK Build-Tools**
   - ✅ **Android SDK Platform-Tools**
   - ✅ **Android SDK Command-line Tools**
4. Нажмите **"Apply"** и **"OK"**

---

## 📂 Открытие проекта в Android Studio

### Вариант 1: Через командную строку (рекомендуется)

```bash
# Из корня проекта
npm run cap:open
```

Эта команда автоматически:
- Соберет веб-приложение
- Синхронизирует Capacitor
- Откроет Android Studio

### Вариант 2: Вручную

1. Запустите Android Studio
2. **File** → **Open**
3. Выберите папку: `D:\project\Workout_Interval_Timer\android`
4. Дождитесь завершения индексации и синхронизации Gradle

---

## 🔨 Сборка APK файла

### Способ 1: Через Android Studio (GUI) - для тестирования

1. **Откройте проект** в Android Studio (см. выше)

2. **Выберите конфигурацию сборки:**
   - В верхней панели найдите выпадающий список рядом с зеленой кнопкой "Run"
   - Выберите **"app"** (если не выбрано)

3. **Соберите Debug APK:**
   - **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
   - Дождитесь завершения сборки (обычно 1-3 минуты)

4. **Найдите APK файл:**
   - После успешной сборки появится уведомление
   - Нажмите **"locate"** в уведомлении
   - Или перейдите вручную: `android\app\build\outputs\apk\debug\app-debug.apk`

### Способ 2: Через командную строку (Gradle)

```bash
# Из корня проекта
cd android
gradlew.bat assembleDebug
```

APK будет в: `android\app\build\outputs\apk\debug\app-debug.apk`

### Способ 3: Используя npm скрипт

```bash
# Из корня проекта
npm run cap:build
```

Это выполнит сборку через Gradle.

---

## 📱 Установка APK на устройство

### Вариант 1: Через USB (ADB)

1. **Включите режим разработчика на Android устройстве:**
   - Откройте **Настройки** → **О телефоне**
   - Нажмите 7 раз на **"Номер сборки"**
   - Вернитесь в **Настройки** → **Параметры разработчика**
   - Включите **"Отладка по USB"**

2. **Подключите устройство к компьютеру:**
   - Используйте USB кабель
   - На устройстве разрешите отладку по USB

3. **Проверьте подключение:**
   ```bash
   adb devices
   ```
   Должно показать ваше устройство

4. **Установите APK:**
   ```bash
   adb install android\app\build\outputs\apk\debug\app-debug.apk
   ```

### Вариант 2: Через Android Studio

1. Подключите устройство по USB (см. выше)
2. В Android Studio нажмите зеленую кнопку **"Run"** (▶️)
3. Выберите ваше устройство из списка
4. Приложение автоматически установится и запустится

### Вариант 3: Вручную (через файловый менеджер)

1. Скопируйте файл `app-debug.apk` на Android устройство
2. Откройте файл на устройстве
3. Разрешите установку из неизвестных источников (если нужно)
4. Нажмите **"Установить"**

---

## 🔧 Решение проблем

### Проблема: Gradle sync failed

**Решение:**
1. Проверьте наличие интернет-соединения
2. Откройте **File** → **Invalidate Caches / Restart** → **Invalidate and Restart**
3. Убедитесь, что `android/local.properties` содержит правильный путь к SDK:
   ```
   sdk.dir=C:\\Users\\<ВашеИмя>\\AppData\\Local\\Android\\Sdk
   ```

### Проблема: "SDK location not found"

**Решение:**
1. Откройте файл `android/local.properties`
2. Убедитесь, что путь к SDK правильный:
   ```
   sdk.dir=C:\\Users\\ВашеИмя\\AppData\\Local\\Android\\Sdk
   ```
3. Используйте двойные обратные слеши `\\` или одинарные прямые `/`

### Проблема: "Build failed" или ошибки компиляции

**Решение:**
1. Очистите проект: **Build** → **Clean Project**
2. Пересоберите: **Build** → **Rebuild Project**
3. Или через командную строку:
   ```bash
   cd android
   gradlew.bat clean
   gradlew.bat assembleDebug
   ```

### Проблема: "Device not found" при установке

**Решение:**
1. Убедитесь, что устройство подключено по USB
2. Включите "Отладку по USB" в настройках разработчика
3. Проверьте драйверы USB (установите через Android Studio SDK Manager)
4. Проверьте команду: `adb devices`

### Проблема: "INSTALL_FAILED" при установке APK

**Решение:**
1. Удалите старое приложение с устройства (если установлено)
2. Проверьте, что на устройстве достаточно места
3. Убедитесь, что включена установка из неизвестных источников

---

## 📝 Полезные команды

```bash
# Собрать веб-приложение и синхронизировать с Android
npm run cap:sync

# Открыть Android Studio
npm run cap:open

# Собрать APK через Gradle
npm run cap:build

# Очистить проект
cd android && gradlew.bat clean

# Проверить подключенные устройства
adb devices

# Установить APK на устройство
adb install android\app\build\outputs\apk\debug\app-debug.apk

# Удалить приложение с устройства
adb uninstall com.workoutintervaltimer.app
```

---

## ✅ Чек-лист перед сборкой APK

- [ ] Android Studio установлен и настроен
- [ ] Проект открыт в Android Studio (папка `android`)
- [ ] Gradle синхронизация завершена успешно
- [ ] Веб-приложение собрано (`npm run build`)
- [ ] Capacitor синхронизирован (`npm run cap:sync`)
- [ ] Файл `android/local.properties` существует и содержит путь к SDK
- [ ] Android SDK Platform 35 установлен
- [ ] Устройство подключено (если нужно установить сразу)

---

## 📦 Структура проекта

```
Workout_Interval_Timer/
├── android/                    # Android проект (открывать в Android Studio)
│   ├── app/
│   │   └── build/
│   │       └── outputs/
│   │           └── apk/
│   │               └── debug/
│   │                   └── app-debug.apk  # ← Здесь будет APK
│   ├── local.properties        # Путь к Android SDK
│   └── build.gradle
├── dist/                       # Собранное веб-приложение
├── capacitor.config.ts         # Конфигурация Capacitor
└── package.json
```

---

## 🎯 Быстрый старт

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Соберите и синхронизируйте:**
   ```bash
   npm run cap:sync
   ```

3. **Откройте в Android Studio:**
   ```bash
   npm run cap:open
   ```

4. **В Android Studio:**
   - Дождитесь синхронизации Gradle
   - **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
   - APK будет в `android\app\build\outputs\apk\debug\app-debug.apk`

---

**Готово!** Теперь у вас есть APK файл для тестирования на реальном устройстве! 🎉












